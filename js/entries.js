/* ============================================================
   entries.js — The Journal

   HOW TO ADD A NEW ENTRY:
   1. Copy the template below.
   2. Paste it right after "const journalEntries = [" so newest
      entries appear first on the page.
   3. Each string inside "paragraphs" becomes its own paragraph.
   4. To attach photos: drop the image files into the images/
      folder, then list their paths in "photos".
      (Or use compose.html — it writes this code for you.)

   TEMPLATE:
   {
     date: "July 23, 2026",
     author: "mike",     // "mike" for your entries, "alysha" for hers
     paragraphs: [
       "First paragraph...",
       "Second paragraph..."
     ],
     photos: ["images/MyPhoto.jpeg"]
   },
   ============================================================ */

const journalEntries = [

  {
    date: "July 25, 2025",
    paragraphs: [
      "Okay, so I've been MIA from our digital journal like a character who disappears mid-season and everyone's like \"where did they go??\" Plot twist: I've been building virtual worlds for work, but the only world that actually matters is the one where you and Kai exist. My coding brain has been running at 110% capacity, but my heart's CPU is always reserved for you two.",
      "Fun fact: Did you know that when programmers get tired, they start thinking in code? Well, if I were to write you in JavaScript, you'd be the function that makes everything else work. You're like the main character in my favorite game — the one I never want to put down, the one that makes all the side quests worth it.",
      "Watching you with Kai is like watching the best co-op gameplay ever. You two are an unstoppable team, and somehow you let me join your party. Kai's got your sense of humor and your way of making everything an adventure. It's like you created the perfect little sidekick, and now I get to be part of this epic storyline.",
      "Here's the thing about building virtual reality: no matter how advanced the graphics get, how immersive the experience becomes, nothing compares to the reality of coming home to you. You're better than any game, any world I could ever code. You're the cheat code that makes life actually fun.",
      "So while I'm out here creating digital universes, just know that you and Kai are my favorite reality. You're the reason I keep grinding through these late-night coding sessions — because every line of code I write is another step toward the future we're building together. You're my favorite Easter egg, my secret level, my perfect game. 🎮💕"
    ],
    photos: []
  },

  {
    date: "July 19, 2025",
    paragraphs: [
      "Last night was one of those nights I'll remember forever. Walking with you, sharing ice cream, laughing at the way it melted faster than we could eat it — it felt like the world was just ours for a little while. I loved our deep talk, the way you opened up and let me see even more of your heart. Every time I look into your eyes, I see so much love shining back at me. It's overwhelming in the best way.",
      "Telling you about my manic bipolar disorder is one of the hardest things I've ever had to do. Living with it feels like carrying a storm inside me that I can't always control. But you... you accept every part of me without hesitation. You don't see it as something to fix or something to fear. You just love me, all of me, even the parts that feel broken. That means more to me than I can ever put into words.",
      "Lying next to you, feeling your warmth, I realize how much peace you bring into my life. There's nowhere I feel safer or more at home than wrapped up with you. Thank you for loving me so deeply, for making even the simplest moments feel magical. I can't wait for a lifetime of nights like this, with you by my side."
    ],
    photos: []
  },

  {
    date: "July 17, 2025",
    paragraphs: [
      "Some weeks, life pulls us in different directions. The days slip by, and sometimes it feels like we're living in parallel worlds—close, but not always together. I want you to know that even in the quiet stretches, you mean the world to me.",
      "Even when we don't see each other as much as I wish, or when our conversations are brief, my heart never drifts. Every morning, I wake up and choose you—again and again, no matter the distance or the busyness of our days.",
      "You are never too far, and I am never too busy for you. I am always here, loving you in the spaces between our time together, holding you close in my thoughts even when I can't hold you close in my arms.",
      "The moments we do share—no matter how small—mean everything to me. I cherish every laugh, every story, every quiet moment by your side. I love discovering your world, sharing in your interests, and seeing the things that light you up like constellations in my sky.",
      "Thank you for being my constant, my joy, and my favorite part of every day. Even when the world feels chaotic, you are my peace. I can't wait for all the moments still to come, together."
    ],
    photos: []
  },

  {
    date: "July 14, 2025",
    paragraphs: [
      "Today feels different. Maybe it's because I've been working on this website for you, pouring pieces of my heart into every line of code. Maybe it's because I finally found a way to say what I've been feeling but couldn't quite put into words. Creating this space for us feels like building something sacred — a digital shrine to everything we are and everything we're becoming.",
      "I love that you write in our physical journal with ink and paper, and I create these glowing pixels for you. We're documenting our love in two different languages, but they're saying the same thing: you matter to me more than I ever thought possible. Every feature I add to this site, every animation, every small detail — it's all just different ways of saying \"I love you.\"",
      "I hope when you see this, you feel how much care went into every pixel. This isn't just a website. This is my love letter to you, written in the only language I know how to speak fluently — code. And somehow, that feels perfect for us."
    ],
    photos: []
  },

  {
    date: "July 5, 2025",
    paragraphs: [
      "Yesterday, we fought like thunder. Words flew that shouldn't have, silence followed that cut deeper than either of us meant. You blocked me — and I can't even blame you for it. But love doesn't just live in easy moments. It lives in the fire, in the storm, in the ache of missing you even when I'm angry.",
      "I couldn't sit with it. I couldn't be okay with not trying. So I ran — literally — to your house, heart pounding harder than my feet, just to say one thing: I love you. Not in a perfect, polished way. In a broken, bleeding, human way. I love you enough to show up even when I'm scared. I love you enough to stay.",
      "If this journal is anything, let it be proof that I don't run from you. I run to you. Always."
    ],
    photos: []
  },

  {
    date: "July 4, 2025",
    paragraphs: [
      "You were working last night. I know you had to — I didn't ask for more. But still, I sat in the quiet with fireworks cracking in the distance, and it felt like the whole world was celebrating while I was just… here. Alone. Missing you.",
      "I watched the sky light up through the window and thought about how different it would feel with your hand in mine. I've never needed big crowds or loud moments — just you. Just your presence. Even your silence is better than the noise this world throws at me.",
      "I thought about us. About someday. About laying on a rooftop, maybe, wrapped in a blanket and watching the sparks — not because we care about the fireworks, but because we finally get to be still together.",
      "I missed you last night in a way that burned soft and deep. But even in the quiet, I felt you. That's what love is, I guess. Being alone, and still knowing someone is yours."
    ],
    photos: []
  },

  {
    date: "July 3, 2025",
    paragraphs: [
      "I've been quiet lately. Not just with you — with myself, with everything. There's been a weight pressing on my chest that I haven't been able to put into words. I didn't mean to shut you out. I wasn't trying to make you feel unloved or alone. I just… didn't know how to speak through the fog I've been walking through.",
      "And then you sent me those flowers. God, they were beautiful. Vibrant, delicate, alive — the opposite of everything I was feeling. They didn't just brighten the room. They cracked something open in me. I stood there holding them, realizing someone still saw me. Still cared. Still loved me even when I wasn't saying anything at all.",
      "So thank you — for your softness, for your patience, for your love. You reached me without a single word. And I hope this journal reaches you the same way."
    ],
    photos: []
  }

];

/* ============================================================
   RENDERER — the journal as a book.
   You land on an INDEX (one line per entry); clicking a line
   opens that entry alone, with Older / Newer page turns.
   Entries without an "author" count as yours ("mike").
   ============================================================ */

let journalFilter = 'all';
let journalSearch = '';
let readingIndex = null;   // null = index view, a number = reading that entry

function visibleEntries() {
  return journalEntries.filter(entry => {
    const author = entry.author || 'mike';
    const voiceOk = journalFilter === 'all' || author === journalFilter;
    if (!voiceOk) return false;
    if (!journalSearch) return true;
    const haystack = (entry.date + ' ' + entry.paragraphs.join(' ')).toLowerCase();
    return haystack.includes(journalSearch);
  });
}

function renderJournal() {
  const container = document.getElementById('entries');
  if (!container) return;
  container.innerHTML = '';

  const visible = visibleEntries();

  if (visible.length === 0) {
    const note = document.createElement('p');
    note.className = 'journal-empty-note';
    note.textContent = journalFilter === 'alysha'
      ? 'Her pages are waiting. 🤍'
      : 'No entries here yet. 🖤';
    container.appendChild(note);
    return;
  }

  if (readingIndex === null) {
    renderIndex(container, visible);
  } else {
    renderReading(container, visible);
  }
}

/* ---------- The index: one line per entry ---------- */

function renderIndex(container, visible) {
  visible.forEach((entry, i) => {
    const author = entry.author || 'mike';

    const row = document.createElement('div');
    row.className = 'index-row' + (author === 'alysha' ? ' hers' : '');

    const date = document.createElement('span');
    date.className = 'index-date';
    date.textContent = (author === 'alysha' ? '🤍 ' : '🖤 ') + entry.date;

    // A whisper of the first line, trimmed
    const preview = document.createElement('span');
    preview.className = 'index-preview';
    const firstLine = (entry.paragraphs[0] || '');
    preview.textContent = firstLine.length > 70
      ? firstLine.slice(0, 70).trim() + '…'
      : firstLine;

    row.appendChild(date);
    row.appendChild(preview);

    row.addEventListener('click', () => {
      readingIndex = i;
      renderJournal();
      window.scrollTo(0, 0);
    });

    container.appendChild(row);
  });
}

/* ---------- The reading view: one entry, like an open page ---------- */

function renderReading(container, visible) {
  // Stay in bounds if the filter changed underneath us
  if (readingIndex >= visible.length) readingIndex = visible.length - 1;
  const entry = visible[readingIndex];
  const author = entry.author || 'mike';

  // Back to the index
  const back = document.createElement('button');
  back.className = 'journal-back';
  back.textContent = '← All entries';
  back.addEventListener('click', () => {
    readingIndex = null;
    renderJournal();
  });
  container.appendChild(back);

  // The entry itself, fully open
  const article = document.createElement('article');
  article.className = 'entry' + (author === 'alysha' ? ' hers' : '');

  const header = document.createElement('h2');
  header.textContent = (author === 'alysha' ? '🤍 ' : '🖤 ') + entry.date;

  const body = document.createElement('div');
  body.className = 'entry-body open';
  body.style.maxHeight = 'none';

  entry.paragraphs.forEach(text => {
    const p = document.createElement('p');
    p.textContent = text;
    body.appendChild(p);
  });

  if (author === 'alysha') {
    const sig = document.createElement('p');
    sig.className = 'her-signature';
    sig.textContent = '— in her words';
    body.appendChild(sig);
  }

  if (entry.photos && entry.photos.length > 0) {
    const photoWrap = document.createElement('div');
    photoWrap.className = 'entry-photos';
    entry.photos.forEach(src => {
      const img = document.createElement('img');
      img.src = src;
      img.alt = 'Memory from ' + entry.date;
      img.className = 'entry-photo';
      img.loading = 'lazy';
      photoWrap.appendChild(img);
    });
    body.appendChild(photoWrap);
  }

  article.appendChild(header);
  article.appendChild(body);
  container.appendChild(article);

  // Edit / Delete — only for live (posted) entries, and only when
  // this device has whispered the word
  const canManage = entry.live && entry.id &&
    typeof currentVoice === 'function' && currentVoice();

  if (canManage) {
    const actions = document.createElement('div');
    actions.className = 'entry-actions';

    const editBtn = document.createElement('button');
    editBtn.className = 'journal-back';
    editBtn.textContent = '✎ Edit';
    editBtn.addEventListener('click', () => {
      if (typeof startEditingEntry === 'function') {
        startEditingEntry(entry);
        window.location.hash = '#write';
      }
    });

    const deleteBtn = document.createElement('button');
    deleteBtn.className = 'journal-back';
    deleteBtn.textContent = '✕ Delete';
    deleteBtn.addEventListener('click', async () => {
      const sure = confirm('Delete this entry forever? This cannot be undone.');
      if (!sure) return;
      await window.journalDB.from('live_entries').delete().eq('id', entry.id);
      readingIndex = null;
      loadLiveEntries();
    });

    actions.appendChild(editBtn);
    actions.appendChild(deleteBtn);
    container.appendChild(actions);
  }

  // Page turns — the list is newest-first, so "older" is further down
  const nav = document.createElement('div');
  nav.className = 'journal-read-nav';

  const newer = document.createElement('button');
  newer.textContent = '← Newer';
  newer.disabled = readingIndex === 0;
  newer.addEventListener('click', () => {
    readingIndex--;
    renderJournal();
    window.scrollTo(0, 0);
  });

  const position = document.createElement('span');
  position.className = 'read-position';
  position.textContent = 'Entry ' + (readingIndex + 1) + ' of ' + visible.length;

  const older = document.createElement('button');
  older.textContent = 'Older →';
  older.disabled = readingIndex === visible.length - 1;
  older.addEventListener('click', () => {
    readingIndex++;
    renderJournal();
    window.scrollTo(0, 0);
  });

  nav.appendChild(newer);
  nav.appendChild(position);
  nav.appendChild(older);
  container.appendChild(nav);
}

// Wire up the All / Mine / Hers filter pills
function setupJournalFilters() {
  const pills = document.querySelectorAll('.journal-filter');
  pills.forEach(pill => {
    pill.addEventListener('click', () => {
      journalFilter = pill.dataset.filter;
      readingIndex = null;   // filters always return you to the index
      pills.forEach(p => p.classList.toggle('active', p === pill));
      renderJournal();
    });
  });
}

/* ============================================================
   LIVE ENTRIES — posts written on the site itself (Supabase).
   These are fetched and woven in among the entries above,
   sorted by date. If Supabase isn't configured, this quietly
   does nothing and the journal works exactly as before.
   ============================================================ */

async function loadLiveEntries() {
  if (!window.journalDB) return;

  const { data, error } = await window.journalDB
    .from('live_entries')
    .select('*')
    .order('created_at', { ascending: false });

  if (error || !data) return;

  // Remove any live entries from a previous load (avoids doubles)
  for (let i = journalEntries.length - 1; i >= 0; i--) {
    if (journalEntries[i].live) journalEntries.splice(i, 1);
  }

  // Convert database rows into journal entries
  data.forEach(row => {
    journalEntries.push({
      id: row.id,
      date: row.entry_date,
      author: row.author,
      live: true,
      photos: Array.isArray(row.photos) ? row.photos : [],
      paragraphs: row.content
        .split(/\n\s*\n/)
        .map(p => p.replace(/\s*\n\s*/g, ' ').trim())
        .filter(p => p.length > 0)
    });
  });

  // Newest first, file entries and live entries woven together
  journalEntries.sort((a, b) => new Date(b.date) - new Date(a.date));

  renderJournal();
}

document.addEventListener('DOMContentLoaded', () => {
  renderJournal();
  setupJournalFilters();
  loadLiveEntries();

  // Search-as-you-type
  const searchBox = document.getElementById('journalSearch');
  if (searchBox) {
    searchBox.addEventListener('input', () => {
      journalSearch = searchBox.value.toLowerCase().trim();
      readingIndex = null;   // searching returns you to the index
      renderJournal();
    });
  }
});
